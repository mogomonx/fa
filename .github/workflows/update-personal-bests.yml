name: Update Personal Bests

# Cheap and fast (a handful of small API calls) -- keeps the FA Records
# page, Event Rankings, and Sum of Ranks / Kinch fresh daily.
on:
  schedule:
    - cron: '0 6 * * *'   # 06:00 UTC every day
  workflow_dispatch: {}
  push:
    branches: [main]
    paths:
      - 'config/members.json'
      - 'scripts/fetch.js'
      - 'scripts/build-rankings.js'
      - 'scripts/lib/**'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Fetch personal bests from the WCA API
        run: node scripts/fetch.js

      - name: Build rankings
        run: node scripts/build-rankings.js

      - name: Commit updated data
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add docs/data/results.json docs/data/rankings.json
          git diff --staged --quiet || git commit -m 'Update personal bests'
          git pull --rebase origin main
          git push
